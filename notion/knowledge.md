### 1. 说说JavaScript中的数据类型？区别？
- 基本数据类型、引用数据类型
- 存储位置
- 传参和赋值
- 比较
- 数据类型的判定
- 类型转换

### 2. javscript数组的常用方法有哪些？
push pop unshift shift splice
concat slice join
find indexOf/lastIndexOf includes findIndex
forEach map filter some every 
reduce reduceRight
sort reverse
Array.from() Array.of() flat(depth) flatMap(callback)

### 3. Javascript字符串的常用方法有哪些？
length charAt(index) charCodeAt(index) at
indexOf/lastIndexOf includes startsWith endsWith match search
slice  substring
replace replaceAll split
trim toUpperCase toLowerCase
repeat padStart padEnd 
raw

### 4. == 和 ===区别，分别在什么情况使用？

### 5. 深拷贝浅拷贝的区别？如何实现一个深拷贝？
- 定义
- 结果
- 方法
- 应用场景

### 6. 说说你对闭包的理解？闭包使用场景

